name: bluebuild
on:
  schedule:
    - cron:
        "00 06 * * *" # build at 06:00 UTC every day
        # (20 minutes after last ublue images start building)
  push:
    branches: [main] # feature branches build via pull_request; only main publishes
    paths-ignore: # don't rebuild if only documentation has changed
      - "**.md"

  pull_request:
  workflow_dispatch: # allow manually triggering builds
concurrency:
  # only run one build at a time
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: true
jobs:
  bluebuild:
    name: Build Custom Image (${{ matrix.recipe }})
    runs-on: ubuntu-latest
    # The signing module derives its baked-in cosign policy from BB_REGISTRY; the
    # action otherwise defaults the host to ghcr.io. Pin it to the real push
    # target so the policy matches forge.waywardinn.com/monolith-os.
    env:
      BB_REGISTRY: forge.waywardinn.com
      BB_REGISTRY_NAMESPACE: monolith-os
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false # stop GH from cancelling all matrix builds if one fails
      matrix:
        recipe:
          # !! Add your recipes here
          - recipe-gnome.yml
          - recipe-gnome-nvidia.yml
    steps:
      # Check out ourselves first so the kernel signing key is in the workspace
      # before the build action runs (it skips its own checkout below).
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      # Materialize the Secure Boot (MOK) private key the recipes mount as a
      # build secret to sign the CachyOS kernel and the nvidia modules. Stored
      # base64-encoded in the KERNEL_SIGNING_SECRET repo secret; the public cert
      # is committed in the image tree. See `just generate-secureboot-key`.
      - name: Extract kernel signing key
        shell: bash
        env:
          KERNEL_SIGNING_SECRET: ${{ secrets.KERNEL_SIGNING_SECRET }}
        run: |
          if [ -z "${KERNEL_SIGNING_SECRET}" ]; then
            echo "::error::KERNEL_SIGNING_SECRET is not set (run 'just generate-secureboot-key' and add it)." >&2
            exit 1
          fi
          echo "${KERNEL_SIGNING_SECRET}" | base64 -d > MOK.priv

      # the build is fully handled by the reusable github action
      - name: Build Custom Image
        # Bare ref — resolves from github.com now that this Forgejo has
        # DEFAULT_ACTIONS_URL=github. Kept bare (not a full URL) so Renovate's
        # github-actions manager bumps it with no extra config.
        # Pin v1.11.1, NOT v1.11: v1.11 is an annotated tag and this Forgejo
        # runner mishandles annotated tags; v1.11.1 is the same commit as a
        # lightweight tag.
        uses: blue-build/github-action@v1.11.1
        with:
          recipe: ${{ matrix.recipe }}
          cosign_private_key: ${{ secrets.SIGNING_SECRET }}
          # Pin the CLI version. Otherwise the action resolves "latest" by curling
          # api.github.com with `Authorization: Bearer ${{ github.token }}` — and
          # github.com 401s on a Forgejo token. A set cli_version skips that lookup.
          cli_version: v0.9.35
          # Push to the self-hosted Forgejo registry under the monolith-os org,
          # authenticating with the Forgejo Actions token.
          registry: forge.waywardinn.com
          registry_namespace: monolith-os
          # The auto Actions token can't write packages to the monolith-os org
          # (401 reqPackageAccess); use a PAT with write:package owned by a user
          # with org access.
          registry_username: TheDarkWizard
          registry_token: ${{ secrets.REGISTRY_TOKEN }}
          pr_event_number: ${{ github.event.number }}
          # We checked out above (to stage MOK.priv); don't let the action wipe it.
          skip_checkout: true
          # Self-hosted runner brings its own disk; no GH-runner space to reclaim.
          maximize_build_space: false

  generate-release:
    # Caller of the reusable generate_release.yml; named distinctly from the
    # callee ("Generate Release") so Forgejo's two rows don't read identically.
    name: Release
    needs: bluebuild
    # Only on main: PRs don't publish images to diff, and pushes to feature
    # branches must not create releases or re-point the release tag.
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
    uses: ./.github/workflows/generate_release.yml
